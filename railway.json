
{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd primes-app && composer install --no-dev && php artisan key:generate",
    "startCommand": "cd primes-app && php artisan serve --host 0.0.0.0 --port $PORT"
  }
}
