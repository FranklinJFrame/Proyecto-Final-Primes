{
  "build": {
    "command": "composer install --no-dev && npm install && npm run build && php artisan key:generate && chmod -R 775 storage bootstrap/cache && php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "start": {
    "command": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}